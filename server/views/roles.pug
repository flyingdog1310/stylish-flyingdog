extends layout.pug

block content
    div(id="header-stuff")
    form(action="/admin/create_role" method="POST" enctype="multipart/form-data" id="create-role")
        p 1=授權 0=未授權 
        p 賦予用戶權限-創商品-創行銷活動--結帳
        p admin=1111 / merchant=0101 / marketing=0011 / user=0001
        label(for="role") Role
        input(id="role" name="role")
        label(for="access") Access
        input(id="access" name="access")
        button(type="submit" class="btn") create role
    form(action="/admin/assign_role" method="POST" enctype="multipart/form-data" id="assign-role")
        label(for="userId") UserId 
        input(id="userId" name="userId")
        label(for="roleId") RoleId
        input(id="roleId" name="roleId")
        button(type="submit" class="btn") assign role
    script. 
        import(`/public/js/layout.js`)   
        import(`/public/js/roles.js`)   